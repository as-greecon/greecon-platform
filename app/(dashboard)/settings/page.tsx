import { EmptyState } from "@/components/empty-state";

export default function SettingsPage() {
  return <EmptyState title="Configure your workspace" description="Preferences, roles, and integrations will live in this area." />;
}
